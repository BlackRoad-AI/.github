name = "blackroad-api-gateway"
main = "cloudflare/api-gateway.js"
compatibility_date = "2024-01-01"

[vars]
AGENT = "LUCIDIA"
ORG   = "BlackRoad-AI"

# Secrets — set via: wrangler secret put <NAME>
#   OPENAI_API_KEY
#   ANTHROPIC_API_KEY
#   LOCAL_GATEWAY_URL   (Cloudflare Tunnel URL for the Pi cluster)
